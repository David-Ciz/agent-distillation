# DVC pipeline parameters
# Used only by the dataset creation stages (create_train_dataset, create_eval_dataset).
# Training and evaluation hyperparameters are passed directly to the SLURM scripts
# via environment variables — see task1/scripts/slurm/train_lora_lumi.sh.
